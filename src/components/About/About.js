import React from 'react';

const About = () => {
    return (
        <div>
            <h2 style={{ textAlign: 'center' }}>Ema John's plan to serve customers this holiday season includes alternate delivery methods and easy returns.</h2>
        </div>
    );
};

export default About;